<Window x:Class="StickerApp.StartupPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="340"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False">

    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.6" BlurRadius="24" ShadowDepth="6"/>
        </Border.Effect>

        <StackPanel Margin="20">
            <TextBlock Text="Создай задачу"
                       Foreground="{StaticResource TextBrush}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       FontFamily="Segoe UI Variable, Segoe UI"/>

            <TextBlock Text="Что запланировано на сегодня?"
                       Foreground="{StaticResource SubtextBrush}"
                       FontSize="11"
                       FontFamily="Segoe UI Variable, Segoe UI"
                       Margin="0,4,0,12"/>

            <Border Height="32"
                    Background="#1C1C2A"
                    BorderBrush="#2A2A38"
                    BorderThickness="1"
                    CornerRadius="5">
                <TextBox x:Name="InputBox"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource TextBrush}"
                         CaretBrush="{StaticResource AccentBrush}"
                         FontSize="12"
                         FontFamily="Segoe UI Variable, Segoe UI"
                         Padding="8,6"
                         VerticalAlignment="Center"/>
            </Border>

            <StackPanel x:Name="ExistingPanel"
                        Visibility="Collapsed"
                        Margin="0,12,0,0">
                <TextBlock Text="Выбери стикер"
                           Foreground="{StaticResource SubtextBrush}"
                           FontSize="11"
                           FontFamily="Segoe UI Variable, Segoe UI"
                           Margin="0,0,0,6"/>
                <ItemsControl x:Name="StickerPicker">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Tag="{Binding}"
                                    Click="OnPickExisting"
                                    Margin="0,0,6,6"
                                    Padding="10,5"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="Bd"
                                                            Background="#1C1C2A"
                                                            BorderBrush="#2A2A38"
                                                            BorderThickness="1"
                                                            CornerRadius="5"
                                                            Padding="{TemplateBinding Padding}">
                                                        <TextBlock Text="{Binding}"
                                                                   Foreground="{StaticResource TextBrush}"
                                                                   FontSize="11"
                                                                   FontFamily="Segoe UI Variable, Segoe UI"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   MaxWidth="200"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="Пропустить"
                        Click="OnSkip"
                        Padding="12,5"
                        Margin="0,0,8,0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{StaticResource SubtextBrush}"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="ExistingBtn"
                        Content="В существующий"
                        Click="OnExistingClick"
                        Padding="12,5"
                        Margin="0,0,8,0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd"
                                                Background="#1C1C2A"
                                                BorderBrush="#2A2A38"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Padding="{TemplateBinding Padding}">
                                            <TextBlock Text="{TemplateBinding Content}"
                                                       Foreground="{StaticResource TextBrush}"
                                                       FontSize="12"
                                                       FontFamily="Segoe UI Variable, Segoe UI"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Opacity" Value="0.78"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Новый стикер"
                        Click="OnNewClick"
                        Padding="12,5"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd"
                                                Background="{StaticResource AccentBrush}"
                                                CornerRadius="5"
                                                Padding="{TemplateBinding Padding}">
                                            <TextBlock Text="{TemplateBinding Content}"
                                                       Foreground="White"
                                                       FontSize="12"
                                                       FontFamily="Segoe UI Variable, Segoe UI"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
